export default class SubscriptionTier {
  constructor(minRange: number, price: number) {}

  get minRange(): number {
    return this.minRange;
  }

  get price(): number {
    return this.price;
  }
}
